class Tournament {
  final int id;
  final String name;
  final String urlProfile;

  // parametros de tipo ordinal
  // Tournament(this.id, this.name, this.urlProfile, this.matchDay);

  // parámetros de tipo nombreado
  Tournament({required this.id, required this.name, required this.urlProfile});
}